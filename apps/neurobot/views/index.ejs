<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Reddit Data Collector</title>
</head>
<body>
  <h1>Welcome, <%= user %>!</h1>
  <h2>Reddit Categories</h2>
  <ul>
    <% categories.forEach(category => { %>
      <li>
        <form action="/fetch-category" method="POST">
          <input type="hidden" name="category" value="<%= category %>">
          <button type="submit">Fetch Posts from <%= category %></button>
        </form>
      </li>
    <% }) %>
  </ul>
</body>
</html>